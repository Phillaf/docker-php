version: '2'
services:

  php:
    container_name: php
    build: docker/php
    volumes:
      - ./:/usr/share/nginx/www

  nginx:
    build: docker/nginx
    links:
      - php
    volumes:
      - ./:/usr/share/nginx/www
    environment:
      VIRTUAL_HOST: backend

networks:
  default:
    external:
      name: myapp
